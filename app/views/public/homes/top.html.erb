<h4>ジャンル検索</h4>
 <% @genres.each do |genre| %>
  <%= genre.name %>
 <% end %>

ようこそ、ながのCAKEへ！
このサイトは、ケーキ販売のECサイトになります。
会員でない方の商品の閲覧はできますが、
購入には会員登録が必要になります。

<h2>新着商品</h2>
 <% @items.each do |item| %>
  <%= item.name %>
  <%= item.id %>
  <%= item.price %>
  <%= link_to attachment_image_tag item, :image, :fill, 200, 200, public_item_path(item.id) %>
  <% end %>

 <%=link_to "全ての商品を見る", public_items_path %>